



@model UserViewModel


<div class="row">
    <div class="col-md-6 offset-md-3">

       <user-picture-thumbnail class="rounded" style="width:300px" picture-url="@Model.PictureUrl"></user-picture-thumbnail>
    </div>
</div>



<div class="row">
   @*  <div class="col-md-4 offset-md-4">
        @if (String.IsNullOrEmpty(Model.PictureUrl))
        {
            <img class="rounded" src="/userpictures/default_user_picture.jpg" />
        }
        else
        {
            <img class="rounded" src="/userpictures/@(Model.PictureUrl)" />
        }
    </div> *@

    <div class="col-md-6 offset-md-2">
      <h1>Kullanici bilgileri</h1>
        <table class="table table-dark">
            <thead>
                <tr>
                    <th>Kullanici Adi</th>
                    <th>Kullanici Email</th>
                    <th>Telefon no</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>@Model.UserName</td>
                    <td>@Model.Email</td>
                    <td>@Model.PhoneNumber</td>
                </tr>
            </tbody>
        </table>
    </div>
</div>